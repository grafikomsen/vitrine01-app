<div class="owl-carousel owl-theme">
    @if ($banners->isNotEmpty())
    @foreach ($banners as $banner)
    <div class="item">
        <div class="card rounded-0 border-0">
            <img src="{{ asset('uploads/banners/'.$banner->image) }}" class="img-fluid rounded-0" alt="...">
            <div class="card-img-overlay d-block align-content-end text-center">
                <h1 class="text-white">{{ $banner->name }}</h1>
                <p class="text-white">{{ $banner->content }}</p>
                <a class="btn btn-primary px-4 rounded-0 mb-5" href="">En savoir plus</a>
            </div>
        </div>
    </div>
    @endforeach
    @endif
</div>
